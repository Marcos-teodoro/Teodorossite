/**
 * LEGADO — Checkout Pro (Express).
 * Use o FastAPI: uvicorn app.main:app --reload --port 3001
 * Ver server/README.md
 */
console.error("[teodora] server/index.js está obsoleto. Rode: uvicorn app.main:app --port 3001");
process.exit(1);
